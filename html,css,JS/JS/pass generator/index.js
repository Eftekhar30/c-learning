//password generator

function generatePassword(length, 
                        includeLowercase,
                        includeUppercase,
                        includeSymbols,
                        includeNumber ){
    
}

const passlength = 12;
const includeLowercase = true;
const includeUppercase = true;
const includeSymbols = true;
const includeNumber = true;

const password = generatePassword(passlength, includeLowercase,includeUppercase,includeSymbols,includeNumber);

console.log(`generated pass: ${password}`);
